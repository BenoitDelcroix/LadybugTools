[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "honeybee-display>=0.6.2",
    "ladybug-vtk>=0.16.1",
    "lbt-dragonfly>=0.12.100",
]

[tool.uv.workspace]
members = [
    "test",
]
